const backdrop = document.getElementById('backdrop'); const label = document.getElementById('scene-label'); const speaker = document.getElementById('speaker'); const text = document.getElementById('text'); const choices = document.getElementById('choices'); let project; let current;
async function start() { const response = await fetch('game.json'); project = await response.json(); current = project.startScene || project.scenes[0]?.id; render(); }
function render() { const scene = project.scenes.find(item => item.id === current) || project.scenes[0]; if (!scene) return; label.textContent = (scene.title || '').toUpperCase(); speaker.textContent = scene.speaker || 'Narrator'; text.textContent = scene.text || ''; backdrop.style.backgroundImage = scene.background ? `url("assets/${encodeURI(scene.background.replace(/^assets\//, ''))}")` : ''; choices.innerHTML = (scene.choices || []).map(choice => `<button data-next="${escapeHtml(choice.next)}">${escapeHtml(choice.text)}</button>`).join(''); choices.querySelectorAll('button').forEach(button => button.addEventListener('click', () => { current = button.dataset.next || current; render(); })); }
function escapeHtml(value) { return String(value ?? '').replace(/[&<>"']/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[character])); } start().catch(error => { text.textContent = error.message; });

