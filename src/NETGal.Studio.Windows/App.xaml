<Application x:Class="NETGal.Studio.Windows.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="CanvasBrush" Color="#0D141D" />
    <SolidColorBrush x:Key="PanelBrush" Color="#111923" />
    <SolidColorBrush x:Key="RaisedBrush" Color="#18212C" />
    <SolidColorBrush x:Key="LineBrush" Color="#293445" />
    <SolidColorBrush x:Key="AccentBrush" Color="#E19B67" />
    <SolidColorBrush x:Key="TextBrush" Color="#E8EDF5" />
    <SolidColorBrush x:Key="MutedBrush" Color="#8894A7" />
    <Style TargetType="Button">
      <Setter Property="Background" Value="{StaticResource RaisedBrush}" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="12,7" />
      <Setter Property="Margin" Value="0,0,8,0" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource RaisedBrush}" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="8,6" />
      <Setter Property="Margin" Value="0,4,0,10" />
    </Style>
  </Application.Resources>
</Application>
